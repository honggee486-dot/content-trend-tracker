# 0.10.61 현재 문서 정합성·Gemini 글감 25개 제한

- 자동·예약 Gemini 글감 분석의 요청당 기본값과 허용 상한을 30개에서 25개로 낮춥니다.
- 실제 `.env`에 더 큰 값이 남아 있어도 프로그램이 요청당 최대 25개로 제한하며 동시 요청 기본값은 1개를 유지합니다.
- README와 `AI_CONTEXT.md`의 오래된 60·65개 설명을 현재 25개 기준과 구분하고, 생성 토큰·65,000토큰 경고·검증 오류 요약 안내를 반영합니다.
- 새 DB의 자동 수집 스케줄러 기본값은 기존 240분을 유지합니다. 기존 DB 저장값과 이미 등록된 Windows 작업은 사용자가 설정 화면에서 명시적으로 변경할 때만 바뀝니다.
- DB 스키마, Gemini 사고 수준 `medium`, 외부 API 호출 방식과 미처리 글감의 다음 실행 유지 규칙은 변경하지 않습니다.
