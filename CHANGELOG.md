## 0.10.109 - 2026-08-12

- 최신 데이터 수집 대시보드에서 자동 정리 조건 확인과 실제 출처 저장 후 정리 실행을 구분하고, 전체 출처 실패와 기존 군집 작업 겹침에서 보존·순위 보류 상태를 완료로 오인하지 않도록 진행 문구를 맞췄습니다.
- deterministic baseline 읽기 전용 진단은 재구성·비교가 불완전하면 일치·불일치 집계를 채택 근거처럼 노출하지 않고, 신뢰 가능한 표본에서는 현재 군집과 baseline의 대표 제목을 사람용 진단에서 직접 비교할 수 있게 했습니다.
- 운영·P2 진단은 동일한 군집 품질 runtime 결과를 재사용해 품질 표본과 baseline을 중복 계산하지 않으며 생산 군집·설정을 자동 변경하지 않습니다.
- 개발 하네스의 시나리오 매핑을 Python 한 곳으로 통합하고 BAT·PowerShell 진입점이 이를 재사용하도록 단순화했으며, `diagnostics` 시나리오에 운영 진단·NAVER/Daum 전체 범위·P2 통합 진입점 검증을 포함했습니다.
- DB 스키마·외부 API·인증·의존성 계약은 변경하지 않으며 실제 DB/WAL/잠금·Parquet·로그·비밀정보·브라우저 인증 상태는 릴리스 대상에서 제외합니다.

## 0.10.108 - 2026-08-12

- 자동 정리를 새 출처 수집 전이 아니라 사용 가능한 출처 저장 완료 뒤 순위 준비 직전에 실행해, 실패 수집 때문에 기존 보존 자료가 먼저 줄어들지 않도록 했습니다.
- 모든 사용 가능한 출처가 실패하면 기존 통합 순위와 보존 자료를 재사용하고 자동 정리·주제 방향 생성을 건너뛰며, 일부 출처가 성공·부분 성공·변경 없음이면 해당 결과를 보존한 채 후속 순위 흐름을 계속합니다.
- 직접 실행·예약/안전 실행·대시보드 백그라운드 실행·Streamlit 런타임이 동일한 수집 후 정리 계약을 사용하고 외부 API 대기 중 장시간 DuckDB 연결을 유지하지 않습니다.
- Public 저장소의 현재 추적 트리에 `.env`, 실제 DB/Parquet, 로그, credential/cookie/key/archive 등 보호 경로가 유입되면 일반 CI에서 실패하도록 `--tracked-only` 지속 안전 게이트를 추가했습니다.
- 기존 CI의 수동 실행과 `contents: read` 최소 권한, 모든 브랜치 push 검증 계약을 유지해 최종 단일 커밋 릴리스 후보도 동일한 CI를 통과하게 합니다.
- DB 스키마·외부 API 계약·의존성을 변경하지 않으며 실제 DB/WAL/잠금·Parquet·로그·비밀정보·브라우저 인증 상태는 릴리스 대상에서 제외합니다.

## 0.10.107 - 2026-08-11

- Gemini 주제 방향의 HTTP 200 부분 응답에서 이미 유효한 결과는 다시 요청하지 않고, 원본 배치의 누락·검증 탈락 ID만 최대 한 번 보강하며 보강 요청 시도도 기존 Gemini 원장에 기록합니다.
- 2단계 군집 희소 응답의 `existing_links`·`new_groups`·`uncertain_nos`를 상호 배타 범주로 명시하고, 신규 그룹은 입력 제목 문자열 대신 그룹 내부 `representative_candidate_no`를 반환해 프로그램이 원본 제목을 복원합니다.
- 신뢰 가능한 최신 군집 품질 표본에 안전 충돌 차단·식별 근거를 사용하는 결정론적 baseline 비교 진단을 읽기 전용으로 추가하되 현재 저장 군집을 정답으로 간주하거나 생산 군집 규칙을 자동 변경하지 않습니다.
- 최근 2단계 군집 요청의 로컬 `tpm_wait_seconds`와 Gemini `rate_limited`·`daily_quota_exhausted`를 분리해 로컬 TPM 대기와 공급자 제한을 같은 원인으로 오판하지 않도록 운영 진단을 보강했습니다.
- 적용 도구·Agent 하네스의 보호 파일과 최종 단일 릴리스 커밋 검증을 보강하고 PowerShell 7 기반 적용 계약을 유지합니다.
- Public 전환 준비를 위해 `.gitignore`와 예제 설정을 보수적으로 정리하고 Git 전체 이력·PR refs·Gitleaks·인코딩 payload 수동 검토를 결합한 읽기 전용 공개 준비 검사를 추가했습니다.
- 이번 누적 변경은 DB 스키마를 변경하지 않으며 실제 DB/WAL/잠금·Parquet·로그·OAuth·쿠키·브라우저 인증 상태를 릴리스 대상에서 제외하고 기존 원본·자료팩·AI 기록·초안·사실 확인·발행 기록과 수동 최종 발행 원칙을 보존합니다.

## 0.10.106 - 2026-08-10

- NAVER·Daum 분석에서 사용자 개수 상한을 제거하고 `trend_lookback_hours` 최근 시간 범위 전체를 사용하되 기존 설정값은 호환용으로 보존합니다.
- 실제 DuckDB를 수정하지 않는 P2 운영·출처 진단과 스냅샷 재사용 경로를 추가해 수집·Gemini·군집 품질 상태를 현재·과거 계약으로 구분해 확인합니다.
- 2단계 군집 진단을 현재 `50,000` 내부 스냅샷 스캔 안전 상한·외부 1회·토큰 기준 순차 분할 계약에 맞추고 과거 고정 배치 이력 호환성을 유지합니다.
- 저장된 최신 군집의 다중 신규·기존 연결·단독 신규·미확정 실제 제목 표본을 읽기 전용으로 재구성하고 작업 원장과 현재 스냅샷이 일치할 때만 신뢰합니다.
- 기존 군집 후보 검색에서 `특징`, `선택`, `포인트`, `총정리` 같은 일반 편집 템플릿과 조사 결합형 표현을 식별 근거에서 제외해 서로 다른 상품 오병합을 차단합니다.
- 오래된 실행 상태와 Gemini 결과를 DB 행 수정 없이 표시·필터링하는 수집 이력 뷰 계약을 보강했습니다.
- 작업별 확인 경로·검증 라우팅, Windows CI, 격리 Chromium Browser Regression과 안전한 개발 하네스를 정비했습니다.
- 기존 DuckDB 데이터·스키마 의미, 원본·자료팩·AI 기록·초안·사실 확인·발행 기록, 부분 성공과 수동 최종 발행 원칙은 유지합니다.

## 0.10.105 - 2026-08-07

- 최신 데이터 수집을 별도 프로세스로 실행하면서 진행률·단계 이력을 공유 파일로 갱신하고, 진행 중에는 관련 버튼을 비활성화하며 완료 후 자동으로 다시 활성화합니다.
- 수집·1차 군집·2차 Gemini 군집·통합 순위 저장·주제 방향 생성을 프로그램 로그에서 서로 다른 단계로 구분하고, 출처 수집 완료 뒤 후속 군집 실패를 출처 실패로 잘못 표시하지 않습니다.
- 일부 군집이 정상 저장되면 전체 미처리 원문이 남아 있어도 현재 저장된 군집의 주제 방향 생성을 이어가고, 남은 미처리는 다음 실행 대상으로 보존합니다.
- 주제 방향 대상 집계에 전체 군집, 추천·검토, 기회 점수 통과, 기존 방향 완료, 생성 필요, 민감 제목, 근거 없음, 이번 생성 대상과 요청 범위 밖 미검사 수를 추가했습니다.
- 군집 저장 전 중복 cluster_id와 연결을 정리해 기본키 충돌이 전체 순위 저장을 취소하지 않도록 보강했습니다.
- 수집·군집 잠금에 프로세스 시작 식별자와 heartbeat/lease를 추가해 Windows PID 재사용과 중단된 잠금의 영구 차단 위험을 줄였습니다.
- Streamlit을 전용 포트 `8518`의 단일 전경 supervisor로 실행하고, 정확한 등록 프로세스 종료와 같은 터미널 소유권을 유지하는 웹 업데이트·재시작 구조를 추가했습니다.
- 기존 DuckDB 스키마와 원본·자료팩·초안·사실 확인·발행 기록, 출처별 부분 성공과 수동 최종 발행 원칙은 유지합니다.

## 0.10.103 - 2026-08-05

- 같은 정규 URL과 안전한 완전 동일 제목을 프로그램이 먼저 묶는 1차 군집 단계를 추가했습니다.
- 최근 미처리 원문 최대 4,000개에서 1차 군집을 만들고 Gemini 3.5 Flash-Lite가 요청당 최대 200개를 2차 분류합니다.
- 앞선 배치에서 완성된 2차 군집만 다음 배치의 기존 군집 후보로 제공하며 기존 군집끼리 자동 병합하지 않습니다.
- 수동 재계산을 별도 백그라운드 프로세스로 실행하고 시험 기본값을 최대 5회·약 1,000개 1차 군집으로 제한했습니다.
- Gemini 요청과 점수 계산 동안 DuckDB 연결을 닫고 각 배치 결과만 짧은 트랜잭션으로 반영합니다.
- 불확실·누락 결과는 두 번 재시도하고 세 번째에도 불확실하면 `needs_review`로 전환합니다.
- 배치별 URL·동일 제목 1차 절감량, 실제 요청·대기 1차 군집, 입력·출력·사고·전체 토큰과 기존 연결·새 군집·불확실·충돌 차단 수를 기록하고 1,000개당 예상 토큰을 표시합니다.
- 정상 AI 경로에서 유사도 최종 군집·조각 재병합을 제거하고 명백한 사실 충돌 검사만 유지했습니다. API 비활성·키 없음·전체 실패 때는 별도 서비스의 비상 결정론적 폴백으로 오프라인 순위 품질만 보존하며 AI 처리 완료로 기록하지 않습니다.

## 0.10.102 - 2026-08-05

- Gemini 3.5 Flash-Lite를 제목 군집의 보조 검토가 아니라 기본 분류 엔진으로 전환했습니다.
- 같은 URL만 먼저 원자 후보로 묶고, Flash-Lite가 같은 사건별 묶음·분리와 대표 제목 생성을 직접 수행합니다.
- 프로그램은 비교 후보 준비와 날짜·회차·제품·금액·주가 방향의 명백한 충돌 차단만 담당합니다.
- 기존 유사도 0.72 최종 군집, 조각 재병합과 대표 제목 점수 보정은 정상 AI 경로에서 실행하지 않습니다.
- 0.10.101의 반복 모델명 감점과 기존 `trend_cluster_review_v1` 쌍 재검토 후처리를 제거했습니다.
- API 키 없음 또는 전체 AI 호출 실패 시에만 기존 유사도 군집을 비상 폴백으로 사용합니다.
- 일부 AI 응답 누락·검증 탈락 후보는 다른 성공 결과를 취소하지 않고 개별 군집으로 보존합니다.
- DB 스키마, 원본, 자료팩, 초안, 사실 확인과 발행 기록은 변경하지 않습니다.

## 0.10.101 - 2026-08-04

- 군집 대표 제목 후보에서 같은 숫자 포함 제품명·모델명이 정확히 반복되거나 짧은 모델명이 긴 모델명 안에 겹치는 경우를 감점합니다.
- `z폴드8`과 `폴드8`처럼 검색 키워드를 나열한 제목보다 사건·수치·행동이 포함된 자연스러운 원문 제목을 우선합니다.
- `아이폰 17 프로`와 `아이폰 17 프로 맥스`처럼 서로 다른 모델을 비교하는 정상 제목은 반복으로 오인하지 않습니다.
- 군집 구성, Gemini 검토 기준, 점수 임계값, DB 스키마와 기존 사용자 기록은 변경하지 않습니다.
- 순위 알고리즘 버전을 17로 올려 다음 순위 재계산에서 대표 제목만 새 규칙으로 갱신합니다.

## 0.10.100 - 2026-08-04

- 설정의 공통 탭 전체에 걸려 있던 가로 오버플로를 탭 목록으로 한정해 긴 본문이 세로로 잘리지 않고 맨 아래까지 스크롤되도록 수정했습니다.
- 설정 화면에 충분한 하단 여백을 추가하고 탭 본문의 세로 오버플로를 명시적으로 허용했습니다.
- 자동화·이력 → 예약 실행에 최근 `background_refresh`의 실제 DB 기록, 신규·갱신·요청 수와 출처별 결과를 표시합니다.
- Windows 작업 스케줄러의 준비 상태와 실제 수집 성공·변경 여부를 구분해 안내합니다.
- 새로 분석할 글감이 없으면 예약 수집이 정상이어도 Gemini 호출 로그가 늘지 않는 조건을 화면에 설명합니다.
- 수집·스케줄러 동작, DB 스키마, API 호출 기준과 기존 사용자 기록은 변경하지 않습니다.

## 0.10.99 - 2026-08-04

- Gemini 군집 검토가 같은 확신도의 `same_event` 후보를 여러 개 반환할 때 후보 딕셔너리를 직접 비교하며 중단되던 오류를 수정했습니다.
- 적용 후보 정렬을 확신도 내림차순과 `pair_id` 오름차순으로 고정해 동일 입력의 처리 순서를 재현 가능하게 했습니다.
- 순위 계산 실패는 DB 교체 전 분석 단계에서 발생하므로 기존 순위·원본·자료팩·초안·발행 기록을 유지합니다.
- DB 스키마, Gemini 모델·호출 한도와 군집 병합 기준은 변경하지 않습니다.

## 0.10.98 - 2026-08-04

- 설정 → AI·품질 진단이 앱에서 이미 열린 DuckDB 연결을 재사용하도록 수정했습니다.
- 같은 DB 파일을 쓰기 연결과 별도의 읽기 전용 연결로 동시에 열어 발생하던 `different configuration` 오류를 제거했습니다.
- Gemini 안정성·사용 로그와 수집 출처 다양성 진단을 같은 연결에서 SELECT 전용으로 표시합니다.
- 앱 연결이 없는 독립 호출과 테스트에서는 기존처럼 읽기 전용 연결을 여는 폴백을 유지합니다.
- DB 스키마, 데이터, API 호출 정책과 수집·제작·발행 기록은 변경하지 않습니다.

## 0.10.97 - 2026-08-04

- 설정의 AI·품질 진단에 Gemini API 모델·기능별 사용 로그를 추가했습니다.
- 3.6 Flash 자동 분석과 3.5 Flash-Lite 자료 정리·군집 검토 호출을 분리해 표시합니다.
- 최근 30일 호출·성공·실패·요청 항목·입력·출력·사고·전체 토큰과 최근 상세 기록을 확인할 수 있습니다.
- 캐시 적중은 외부 API 사용량에서 제외하며 과거 DB의 선택적 토큰 컬럼이 없어도 읽기 전용으로 동작합니다.
- 표시값은 프로그램이 로컬에 기록한 사용 이력이며 Google AI Studio의 공식 남은 RPM·RPD·결제 사용량은 아닙니다.
- DB 스키마와 기존 수집·자료팩·초안·발행 기록은 변경하지 않습니다.

## 0.10.96 - 2026-08-04

- 자료팩 화면의 Gemini 직접 초안 생성 경로를 제거하고 외부 AI 요청서 흐름으로 단순화했습니다.
- 기존 수동 초안 모델 선택값을 보존하면서 설정 명칭을 `Gemini 자료 정리·군집 검토 모델`로 전환했습니다.
- 결정론 군집 뒤 의미가 애매한 후보만 Gemini로 묶음 검토하고 90점 이상 같은 사건 판정만 병합합니다.
- 날짜·회차·제품·금액과 주가 상승·하락 충돌은 AI 판정 뒤에도 프로그램이 다시 차단합니다.
- Gemini 실패·한도 소진·응답 오류가 발생하면 기존 알고리즘 군집과 출처 수집 성공을 보존합니다.
- DB 스키마, 원본 데이터와 기존 자료팩·초안·발행 기록은 변경하지 않습니다.

## 0.10.95 - 2026-08-04

- `제1235회`와 `1235회`를 같은 회차 식별값으로 정규화했습니다.
- 3자리 이상 고유 회차 후보를 빈도 기반 후보 제한보다 우선해 같은 로또 회차가 여러 군집으로 갈라지는 문제를 보강했습니다.
- 같은 회차와 겹치는 대상 표현에만 유사도 하한을 적용하고 다른 회차·다른 대상의 오병합 방지는 유지했습니다.
- 순위 알고리즘 버전을 15로 올려 기존 저장 순위를 새 군집 규칙으로 재계산합니다.
- DB 스키마, 원본 수집 데이터와 기존 제작·발행 기록은 변경하지 않습니다.

## 0.10.94 - 2026-08-03

- 설정 화면 위에 먼저 표시되던 Gemini 안정성·출처 다양성·군집 사례 진단을 `AI·품질 → 품질·운영 진단` 탭 안으로 옮겼습니다.
- 데이터베이스 백업·안전 복구를 `데이터·연동 → 백업·복구` 탭 안으로 옮겼습니다.
- 설정 상위 메뉴와 보조 탭을 가운데 정렬하고 최소 폭을 12.5rem으로 넓혀 긴 문구가 잘리지 않게 했습니다.
- 좁은 화면에서는 탭 문구를 줄바꿈하지 않고 가로 스크롤로 확인합니다.
- DB 스키마, 기존 진단 계산과 백업·복구 안전 절차는 변경하지 않습니다.

## 0.10.93 - 2026-08-03

- 발행 채널 설정에 고정 프로필 5개의 전체·플랫폼별 연결 준비도를 표시합니다.
- 저장된 주소를 연결 완료, 미설정, 주소 확인 필요로 구분하고 프로필별 수정 경로를 안내합니다.
- 발행 보조에서도 선택 프로필의 준비 상태를 먼저 표시하며 준비되지 않은 프로필은 글쓰기 버튼 전에 설정을 안내합니다.
- 진단은 저장된 주소 형식만 읽으며 외부 접속·로그인·쿠키 확인을 수행하지 않습니다.
- DB 스키마, 기존 프로필·초안·발행 기록과 자동 로그인 금지 원칙은 변경하지 않습니다.

## 0.10.92 - 2026-08-03

- 발행 보조 상단과 단계 카드에 Blogger 파랑, 네이버 초록, 티스토리 주황의 플랫폼 색상을 적용했습니다.
- 준비 저장은 조건부로 구분하고 전달 데이터 복사, 글쓰기 편집기 열기, 확장 입력, 최종 확인은 필수 단계로 표시했습니다.
- 개별 복사·미리보기·호환성 검사와 Blogger API 초안 기능을 선택 영역으로 접어 기본 순서를 단순화했습니다.
- Blogger와 네이버는 실제 새 글 편집기 주소를 저장하고, 티스토리는 개인 블로그 홈 주소에서 새 글 주소를 자동 생성합니다.
- DB 스키마, 기존 초안·발행 기록, 자동 로그인 금지와 수동 최종 발행 원칙은 변경하지 않습니다.

## 0.10.91 - 2026-08-03

- 티스토리는 개인 블로그 주소 하나만 저장하면 실제 새 글 편집기 `/manage/newpost` 주소를 자동 생성하도록 단순화했습니다.
- 발행 보조를 준비 저장 → 전달 데이터 복사 → 글쓰기 화면 열기 → 확장 진단·입력 → 직접 확인의 5단계로 재구성했습니다.
- 기본 화면의 별도 로그인 버튼을 문제 해결 도움말 안으로 옮기고, 주요 주소·입력·버튼에 도움말을 추가했습니다.
- 기존 DB 스키마, 초안, 발행 기록과 Chrome 확장의 권한·게시 안전 경계는 변경하지 않습니다.

## 0.10.90 - 2026-08-03

- 설정 진입 시 `기본 설정`과 기존 기본 입력값을 먼저 표시하고 `AI·품질`, `발행 채널`, `자동화·이력`, `데이터·연동`을 상단 메뉴로 구분한 현재 화면 구조를 운영 문서에 반영했습니다.
- 실제 블로그 연결과 발행 보조를 Blogger 3개·네이버 1개·티스토리 1개의 고정 활성 프로필 5개로 정리하고 플랫폼별 파랑·녹색·주황색 구분, 관리형 추천 전략 4개와 실제 프로필 5개의 차이를 명시했습니다.
- 추가·중복·이전 플랫폼 프로필은 과거 초안과 발행 기록 보호를 위해 물리 삭제하지 않고 비활성 보관한다는 호환 규칙을 `AI_CONTEXT.md`, 운영 문서와 다음 작업 문서에 맞췄습니다.
- 문서와 `VERSION`의 버전·채널 구성·설정 구조가 다시 어긋나지 않도록 릴리스 문서 정합성 회귀 테스트를 추가했습니다. DB 스키마와 실제 사용자 데이터는 변경하지 않습니다.

## 0.10.89 - 2026-08-02

- Blogger 3개·네이버 1개의 기본 발행 전략, SEO·이미지 준비, Chrome 편집기 입력 보조, Blogger 비공개 초안 전송과 7일·30일·90일 성과 비교 흐름을 통합했습니다.
- 발행 전략과 실제 블로그 프로필의 역할을 화면에서 분리하고 설정을 SaaS형 상단 메뉴와 하위 탭으로 재구성했습니다.

## 0.10.88 - 2026-08-02

- 모든 화면 상단에 `VERSION` 파일 기준 현재 앱 버전을 표시하고 브라우저 탭 제목에도 같은 버전을 노출합니다.
- `VERSION` 파일이 누락되거나 형식이 잘못돼도 앱을 중단하지 않고 `버전 확인 불가`로 안전하게 표시합니다.
- DB, Gemini 요청, 수집, 군집, 스케줄러와 로컬 운영 설정은 변경하지 않습니다.

## 0.10.87 - 2026-08-02

- 설정 화면에 주제 방향 v6 품질·운영 진단을 추가해 계약 완전성, 3방향 점수 정렬, 원문 근거 연결률, 검색어 간단성, 분석 대기 글감을 읽기 전용으로 확인합니다.
- Gemini 호출 이력에 선택적 `feature_version`을 추가해 주제 방향 v6 호출을 이전 계약과 구분하고, 성공 요청 4회·요청 글감 60개를 최소 운영 표본으로 표시합니다.
- v6 호출의 현재 15개·사고 수준·600초 조건 일치, 검증 실패·재시도·생성 토큰·소요 시간을 함께 보여 주며 외부 API와 운영 설정은 자동 변경하지 않습니다.

## 0.10.86 - 2026-08-02

- Gemini 글감 방향에 검색 의도, 독자 질문, 입력 신호 기반 수요 근거와 요청 내 근거 ID를 추가하고, 발견 검색어·반복 포착·제공된 정량 신호를 URL 없이 전달합니다.
- 방향별 다섯 하위 점수를 범위 검증한 뒤 Python이 100점 총점을 계산하여 안정 정렬합니다. 동점은 Gemini 원래 순서를 유지하며 요청에 없는 근거 ID는 저장하지 않습니다.
- 기존 방향 데이터는 점수 없음 상태로 계속 표시하고, 완성된 이전 프로필을 일괄 재생성하지 않습니다. 신규 설치 기본값은 요청당 15개·자동 수집 180분으로 조정하며 허용 상한 30개는 유지합니다.

## 0.10.85 - 2026-08-01

- Gemini 글감 생성 안정성 패널에 `오류·재시도 세부 현황` UI를 구현하여 Rate limit 영향 요청 묶음, 재시도 복구, 최종 실패, 재시도 대기 시도(429 retrying), 일일 Quota(daily_quota_exhausted), Timeout, 네트워크 오류, 서버 오류(5xx), 응답 검증 실패, MAX_TOKENS, 잘못된 요청(400) 및 재시도 누적 대기시간(총합·평균·최대)을 명확히 세분화하여 시각화합니다.
- `retrying` 시도 행 수와 실제 영향받은 요청 묶음 수를 별도 카드로 분리 표출하며, 실행 간 지속 cooldown 상태는 없음을 명시적인 안내 캡션으로 전달합니다.
- DB 스키마, API 재시도 정책 및 런타임 백오프 로직은 유지하며 읽기 전용 텔레메트리 표현만 확장합니다.

## 0.10.84 - 2026-08-01

- Gemini API 시도마다 실제 요청 글감 수, 설정 상한, 사고 수준과 HTTP 제한 시간을 기록합니다. 직접 초안 호출은 글감 수를 기록하지 않습니다.
- HTTP 200 응답의 첫 후보에서 `finishReason`과 선택적 `finishMessage`를 안전하게 저장하고, `MAX_TOKENS` 종료를 일반 응답 검증 실패와 별도로 진단합니다.
- 안정성 추천과 기존 Gemini 호출 이력 화면에 실제 요청량·사고 수준·종료 사유 집계를 추가했습니다. 과거 호출의 새 메타데이터가 비어 있어도 기존 추천 계산은 유지됩니다.
- 추가형 DuckDB 마이그레이션만 사용하며 기존 DB·자료팩·초안·발행 기록, 외부 API 호출 수, 프롬프트와 응답 스키마를 변경하지 않습니다.

## 0.10.57 - 2026-07-30

- Gemini 자동·예약 글감 분석의 요청당 기본값과 허용 상한을 65개에서 60개로 조정했습니다. 동시 요청은 1개를 유지하며 남은 후보는 다음 실행에서 이어서 처리합니다.
- 글감 분석 스키마를 버전 5로 올려 게시 시급성·우선순위·권장 유효기간·작성 전 재확인 여부, 필요한 공식 근거·현재 부족한 근거·공식 검색어를 함께 생성합니다.
- 세 글쓰기 방향은 추천 순서로 저장하고 첫 번째 방향의 추천 이유를 별도로 보존합니다. 기존 프로필은 삭제하지 않고 다음 자동 분석에서 상위 대상부터 새 버전으로 보완합니다.
- 새 분석 정보는 기존 `content_plan_json`에 추가해 DB 스키마와 기존 초안·사용자 저장 설정을 유지하며, 오늘의 트렌드 상세과 AI 요청서 사실 확인 기본값에서 활용합니다.

## 0.10.56 - 2026-07-27

- Gemini 글감 자동 분석의 기본 요청당 처리량을 50개에서 65개로 조정했습니다. 동시 요청 기본값은 1개로 유지하며 남은 후보는 다음 실행에서 이어서 처리합니다.
- `.env.example`, 코드 기본값·허용 상한, 화면 안내, 문서와 회귀 테스트를 모두 65개 기준으로 맞췄습니다.
- 실제 `.env`에 `GEMINI_TOPIC_ANGLE_ITEMS_PER_REQUEST`가 별도로 있으면 해당 값이 우선하므로 사용자가 65로 맞춰야 합니다. 비밀정보 파일은 수정 ZIP에 포함하지 않습니다.

## 0.10.55 - 2026-07-27

- AI 요청서의 `ChatGPT에서 요청하기`가 현재 요청서를 클립보드에 복사하고 ChatGPT 웹페이지를 새 탭으로 엽니다.
- ChatGPT 입력·전송과 답변 수집은 자동화하지 않으며, 사용자가 JSON 답변을 직접 복사한 뒤 `ChatGPT 결과 붙여넣기로 이동`에서 검사·저장하도록 안내합니다.
- 별도의 요청서 다시 복사 버튼은 추가하지 않고, 기존 복사 컴포넌트의 키를 안전한 DOM 식별자로 변환해 UUID가 포함된 자료팩에서도 안정적으로 동작하도록 보완했습니다.

## 0.10.54 - 2026-07-27

- Gemini 글감 자동 분석이 표시 제목·요약·확인 항목·방향 3개뿐 아니라 독자 대상, 글 목적, 카테고리, 권장 분량, 주제별 제목 원칙, 본문 구성, 금지 표현을 포함한 `content_plan`을 함께 생성하고 저장합니다.
- 오늘의 트렌드 상세에서 저장된 추천 작성 설정을 확인할 수 있으며, 해당 글감을 AI 요청서로 넘기면 화면의 작성 항목을 자동으로 채웁니다.
- AI 요청서를 저장할 때 사용자가 수정한 독자·목적·관점·분량·규칙·구성·확인 항목을 `topic_content_preferences`에 글감별로 보존하고, 다음에 같은 주제를 열면 마지막 설정을 다시 불러옵니다.
- 기존 `trend_cluster_ai_profiles`에는 `content_plan_json`을 추가형 마이그레이션으로 보완하고, 기존 초안·자료팩·사실 확인 기록은 변경하지 않습니다.
- 풍부해진 구조화 출력을 안정적으로 받기 위해 한 번의 자동 분석 대상을 상위 50개로 줄였습니다. 순위 후보 자체는 삭제하지 않으며 다음 실행에서 남은 대상을 이어서 처리합니다.

## 0.10.53 - 2026-07-27

- 설정 화면에서 Gemini 공식 모델 목록을 조회해 `generateContent` 지원 텍스트 모델만 저장하고, 마지막 정상 목록과 조회 시각을 기존 `app_settings`에 보존하도록 추가했습니다.
- 자동·예약 글감 분석 모델과 수동 최종 초안 모델을 각각 선택해 DuckDB 설정에 저장하고, 프로그램 재실행 뒤에도 기존 선택을 유지합니다.
- 오늘의 트렌드 화면에서 `최신 데이터 수집·분석` 실행 전에 자동 분석 모델을 바로 확인·변경할 수 있으며, Windows 예약 수집도 같은 저장 모델을 읽습니다.
- 예약·BAT 실행은 한 번에 최대 100개 후보를 Gemini 요청 한 묶음으로 처리하고, 새 설치의 스케줄러 기본 입력값을 240분으로 변경했습니다.
- 설정 화면의 Gemini 사용량을 선택 모델별로 나누고, 모델 목록의 토큰 한도와 알려진 RPM·TPM·RPD 참고값을 함께 표시합니다.
- 모델 목록 조회 실패 시 기존 캐시와 선택값을 유지하고, API 키는 기존처럼 `.env`에서만 읽어 DuckDB에 저장하지 않습니다.
- 외부 API를 호출할 수 있는 버튼에 공통 API 아이콘을 표시하고, 요청서 생성·형식 검사 같은 로컬 처리 버튼과 구분했습니다.
- `AI 결과 가져오기`의 결과 생성 출처 기본값을 ChatGPT로 변경하고, 이 값은 API 모델 선택이 아닌 기록용임을 안내합니다.
- 설정의 `수동 최종 초안·검토 모델` 명칭을 실제 기능에 맞게 `Gemini 직접 초안 생성 모델`로 정정했습니다.

## 0.10.52 - 2026-07-26

- 예약 수집과 화면의 `최신 데이터 수집·분석`이 설정·쿼터 확인, 출처별 저장, 순위 저장 구간에서만 DuckDB를 짧게 열도록 변경했습니다.
- NAVER·Daum·Google Trends·Wikimedia 네트워크 요청과 YouTube 교환 파일 읽기 중에는 메인 DuckDB 연결을 닫고, 출처별 결과를 메모리에 받은 뒤 묶음 저장합니다.
- 군집·순위 재계산은 `원본 읽기 → DB 없이 계산 → 결과 트랜잭션 저장`, Gemini 글감 분석은 `대상 읽기 → DB 없이 API 호출 → 결과 저장` 단계로 분리했습니다.
- 프로그램의 수동 YouTube 가져오기도 교환 파일·외부 DuckDB를 먼저 읽고 메인 DB에는 결과만 짧게 저장합니다.
- 짧은 저장 구간과 앱 시작이 겹치면 DuckDB 연결을 최대 약 5초 재시도하고, 계속 잠겨 있으면 내부 traceback 대신 다시 시도 안내를 표시합니다.

## 0.10.51 - 2026-07-26

- Gemini 사용량 카드의 요청·입력·출력 한도를 사용값과 분리해 카드 제목 오른쪽에 표시하고, 사용값에는 실제 사용량만 남겼습니다.
- 자동 수집 스케줄러, 설정 최대치, 실제 실행 이력, 최근 수집 이력, 데이터 보관, NAVER·카카오·무료 공개 데이터, YouTube 교환 파일 상태를 모두 테두리 카드로 통일했습니다.
- 설정 화면의 모든 상태 카드에 물음표 도움말을 추가해 집계 범위, 한도 기준, 로컬 기록과 제공자 콘솔의 차이를 설명합니다.

## 0.10.50 - 2026-07-26

- 오늘 Gemini 사용량의 입력·출력 카드를 하루 누적값에서 그날의 `1회 최대 입력 토큰`과 `1회 최대 출력 토큰`으로 변경했습니다.
- API 요청·최대 입력·최대 출력·사고·전체 토큰 카드에 마우스를 올려 확인할 수 있는 도움말을 추가했습니다.
- 최근 Gemini 호출 상세 표의 입력·출력·사고·전체 토큰에 천 단위 쉼표를 적용했습니다.

## 0.10.49 - 2026-07-26

- Gemini API 요청마다 보낸 텍스트와 받은 응답의 전체 글자·공백 제외 글자·한글 글자 수를 기록하고, 입력·출력·사고·전체 토큰을 함께 표시합니다.
- 설정 화면에 오늘의 실제 API 요청 횟수와 토큰 합계, 모델별 입력·출력 한도, 사용자가 조절할 수 있는 일일 요청 참고 한도를 추가했습니다.
- 기존 Gemini 호출 기록과 DuckDB 데이터를 유지하는 추가형 컬럼 마이그레이션을 적용하며, 과거 기록에는 새 글자 수가 없음을 구분해 표시합니다.

## 0.10.48 - 2026-07-26

- 예약·BAT 수집 뒤 실행된 Gemini 글감 분석을 최근 수집 실행 이력의 `Gemini 주제 방향` 상세 행으로 정확히 기록하도록 수정했습니다.
- Gemini의 실제 요청·재시도 수, 소요 시간, 저장 글감·방향·미처리 개수를 표시하고, 일반 출처의 신규·갱신 합계와 섞이지 않도록 분리했습니다.
- Gemini API 키 없음·새 분석 없음은 정상 생략으로 기록하고, Gemini 실패는 상세 경고로 남기되 출처 수집의 전체 성공 판정을 바꾸지 않습니다.

## 0.10.47 - 2026-07-26

- 예약·BAT 수집 후 Gemini 자동 분석 처리량을 기본 25개 1묶음에서 25개씩 최대 2묶음, 총 50개로 확대했습니다.
- 병렬 요청 최대값을 1로 낮춘 환경에서는 해당 설정을 존중해 한 묶음만 실행하도록 안전하게 제한했습니다.
- 화면에서 직접 실행하는 Gemini 분석의 최대 4묶음 동작과 출처 수집 실패 격리 규칙은 변경하지 않았습니다.

## 0.10.46 - 2026-07-26

- 예약·BAT 수집이 출처 수집과 군집·점수·순위 계산을 마친 뒤, Gemini 분석이 없는 글감 기회 기준 이상 후보를 자동으로 보완하도록 연결했습니다.
- 백그라운드 실행 한 번에는 Gemini 글감 분석을 기본 요청 크기 1묶음까지만 처리해 1시간 스케줄에서 API 사용량이 갑자기 커지지 않도록 제한했습니다.
- Gemini API 키가 없거나 새 분석 대상이 없으면 외부 요청 없이 넘어가며, Gemini 오류는 수집 성공을 실패로 바꾸지 않고 실행 결과의 경고로만 기록합니다.
- 백그라운드 출력과 수집 실행 결과에 Gemini 대상·저장 글감·방향 개수를 함께 남기도록 보완했습니다.

## 0.10.45 - 2026-07-26

- 오늘의 트렌드 기본 정렬을 트렌드 점수보다 실제 글 작성 가치에 가까운 `글감 기회` 순으로 변경하고, 급상승·자료 완성도·최근 확인 정렬을 선택할 수 있게 했습니다.
- 후보 표의 모호한 `점수`·`종합` 머리글을 `트렌드`·`기회`로 바꾸고, 기본 정렬 기준인 글감 기회 점수를 목록에서 바로 비교할 수 있게 했습니다.
- 사실 위험을 민감 분야 단어뿐 아니라 순위·일정·가격·날씨 같은 시점 의존 표현, 날짜·금액·비율 주장, 독립 사실 근거 부족까지 반영하는 0~30점으로 세분화했습니다.
- 점수 규칙 버전을 올려 다음 수집 또는 수동 순위 재계산 때 새 위험 계산이 기존 DB에 안전하게 반영되도록 했습니다.

## 0.10.44 - 2026-07-26

- Deploy 툴바를 내비게이션 버튼 박스 안이 아니라 Streamlit 기본 헤더의 오른쪽 위 영역으로 되돌려, 투명 툴바 레이어가 메뉴 버튼 상단의 클릭을 가로채지 않도록 수정했습니다.
- 현재 정상인 메뉴 클릭, Deploy 표시, sticky 내비게이션 구조는 유지하고 Deploy와 메뉴의 물리적 클릭 영역만 분리했습니다.

## 0.10.43 - 2026-07-26

- 메뉴 클릭과 Streamlit Deploy 표시가 모두 유지되는 현재 구조는 그대로 두고, Deploy 툴바의 세로 위치만 예약된 마지막 메뉴 열과 맞도록 내렸습니다.
- Deploy 글자와 상단 메뉴 글자의 높이를 맞추면서 내비게이션의 sticky 배치, 클릭 이벤트, 오른쪽 스크롤바 비침범 동작은 변경하지 않았습니다.

## 0.10.42 - 2026-07-26

- 고정 상단 내비게이션 아래의 본문 시작 여백을 줄여 `오늘의 트렌드` 등 현재 화면 제목이 메뉴에 더 가깝게 표시되도록 조정했습니다.
- 상단 메뉴와 Streamlit Deploy·더보기 영역의 기존 겹침 방지 공간과 정렬은 그대로 유지합니다.

## 0.10.41 - 2026-07-26

- Gemini 병렬 분석 진행 문구를 전체 글감·응답 완료·처리 중 개수 중심으로 단순화
- 요청이 완료 순서와 다르게 끝나도 각 묶음의 상태를 명확히 표시
- 모호한 `남은 최대 상한` 표현을 `제한까지 최대`로 변경
- Gemini 처리 중에는 추정 퍼센트 문구를 숨기고 실제 요청 상태를 우선 표시

## 0.10.40

- 상단 메뉴 버튼의 좌우 폭·내부 여백·간격을 줄이고 오른쪽에 빈 완충 열을 추가해 좁은 데스크톱 화면에서도 `수집·사용량`이 Streamlit Deploy·더보기 영역을 침범하지 않도록 수정했습니다.
- 상단 메뉴 자체의 오른쪽 예약 공간을 확대하고 넘치는 내부 요소를 잘라, 브라우저 폭과 관계없이 메뉴가 네이티브 툴바 위로 밀려나오지 않도록 보강했습니다.
- Streamlit Deploy·더보기 툴바를 살짝 아래로 맞춰 `수집·사용량` 버튼과 같은 높이의 한 줄로 보이도록 정렬했습니다.

## 0.10.39

- Gemini 테스트 대체 응답처럼 요청이 즉시 끝나는 경우에도 진행 메시지에 `요청별 최대 06:00` 제한 시간이 항상 표시되도록 보완했습니다.
- 실제 API 호출 중 경과 시간·남은 상한 표시 동작은 유지하고, 진행률 회귀 테스트가 실행 속도와 무관하게 안정적으로 통과하도록 수정했습니다.

## 0.10.38

- 코드에 별도 종합점수가 없음을 확인하고, Gemini 자동 분석 최소 기준을 단일 트렌드 점수에서 글감 기회 점수로 변경했습니다.
- 트렌드는 낮아도 정보성·자료 깊이가 충분한 글감은 분석할 수 있고, 트렌드는 높아도 글로 발전시킬 가치가 낮은 후보는 API 대상에서 제외합니다.
- 새 환경변수 `GEMINI_TOPIC_ANGLE_MIN_OPPORTUNITY_SCORE`를 추가하고 기존 `GEMINI_TOPIC_ANGLE_MIN_TREND_SCORE`는 새 값이 없을 때만 호환용으로 읽습니다.
- 선택 글감의 자동 분석 제외 사유와 실행 결과 요약에 글감 기회 점수 기준을 명확히 표시합니다.
- 상단 메뉴의 오른쪽 예약 공간과 버튼 간격·패딩을 조정해 Streamlit Deploy·더보기 버튼과 겹치지 않도록 보정했습니다.

## 0.10.37

- Gemini 글감 자동 분석을 표시 제목, 핵심 요약, 발행 전 확인 항목 3~5개, 주제 방향 3개를 한 번에 생성·저장하는 구조로 확장했습니다.
- 기존 원본 군집 제목과 규칙 기반 점수·순위·출처 집계는 그대로 보존하고, Gemini 결과는 별도 프로필 테이블에 추가 저장합니다.
- 주제 방향 요청을 기본 25개씩 최대 4묶음으로 나누고 5초 간격으로 시작해, 필요한 요청 수만 병렬 실행하도록 변경했습니다.
- API 호출 중 전체 경과 시간, 요청별 경과 시간, 360초 제한과 남은 상한, 시작 대기·완료·실패 상태를 진행 게이지에 실시간 표시합니다.
- 트렌드 점수 50점 미만 글감은 기본적으로 Gemini 자동 분석에서 제외하고, 선택 화면에 제외 이유를 표시합니다.
- 선택 글감의 `콘텐츠 품질` 표기를 실제 의미에 맞게 `자료 완성도`로 변경했습니다.

## 0.10.35

- Gemini Developer API에서 지원하지 않는 Interactions 요청의 `labels` 필드를 제거해 `400 BadRequest`를 수정했습니다.
- `GEMINI_APP_ID`, `feature_id`, 요청 해시는 Google 요청 본문에 보내지 않고 기존 로컬 호출 로그와 캐시 구분에만 사용하도록 정리했습니다.
- 안정 모델 `gemini-3.6-flash`에 맞춰 Interactions REST 엔드포인트를 안정 버전 `v1/interactions`로 변경했습니다.
- Gemini 요청 회귀 테스트에 금지 필드 `labels` 미포함과 안정 API 경로 검사를 추가했습니다.

## 0.10.34

- 기존 로컬 DuckDB의 `topics` 테이블에 `source_count` 컬럼이 없을 때 앱 시작 시 안전하게 추가하고, `topic_source_links` 기준으로 값을 다시 채우도록 호환 마이그레이션을 보강했습니다.
- 저장 자료 정리 후 Gemini 주제 방향 대상 조회에서 발생하던 `Binder Error: Referenced column source_count not found` 오류를 수정했습니다.

# 0.10.30

- 검색어별 원문 발견 이력을 `collection_query_discoveries` 원장에 실행·출처·검색어·원문 단위로 누적 저장
- `source_items.metadata_json`이 재수집으로 갱신되어도 과거 검색어, 신규 저장 여부, 발견 시각과 결과 순위를 보존
- 동일 실행·출처·검색어·원문의 중복 행을 방지하고 가장 높은 검색 결과 순위와 신규 저장 여부를 보수적으로 병합
- 기존 원문과 과거 수집 기록은 부정확하게 역산하지 않으며 현재 군집·추천 상태는 `source_item_id` 조인으로 분석 가능하게 유지
- API 호출량과 수집 주기는 변경하지 않고 DuckDB 추가형 마이그레이션과 관련 회귀 테스트를 추가

# 0.10.29

- NAVER 뉴스·블로그와 Daum 웹문서·카페가 최근 자료량 차이 때문에 분석 표본에서 밀리지 않도록 세부 유형별 최소 몫을 보장
- 동일 검색어가 포털 분석 한도를 과도하게 차지하지 않도록 검색어별 소프트 상한을 적용하고, 자료가 부족할 때는 최신순으로 남은 자리를 채워 기존 분석 건수를 유지
- 포털 분석 한도와 API 호출량·수집 주기·DuckDB 스키마는 변경하지 않고 랭킹 알고리즘 버전을 13으로 갱신
- 세부 유형 균형과 검색어 편중 제한에 대한 회귀 테스트 추가

# 0.10.28

- Google Trends RSS 항목이 같은 피드 URL을 공유해 하나의 원문으로 덮어써지던 문제를 항목별 고유 탐색 URL로 수정
- YouTube 동적 검색어에서 `지역 인기: KR / Gaming` 같은 수집 범위명을 제외하고 실제 영상 제목·구체적 주제를 우선
- `daily`, `highlights`, `viral`, `how`처럼 포털 확장 가치가 낮은 일반 표현을 동적 검색어에서 제외
- 포털 검색 결과의 `seed_kind`가 선택된 동적 검색어 전체 범위와 일치하도록 분류 범위 보정
- Google Trends 고유 URL과 YouTube 검색어 선별 회귀 테스트 추가

# 0.10.27

- 한 글자 한국어 조사를 안전하게 정규화하고 사건 행위·대상·금액·제품 근거를 이용해 동일 사건의 분리 군집을 보수적으로 병합
- 인물명 검색어만 같은 서로 다른 사건과 금액·제품이 충돌하는 사건은 병합하지 않도록 제한
- 독립 원문에서 반복되는 구체적인 사건 제목을 대표 제목으로 우선하고 인물·기업·프로그램명만 남은 제목은 추천 제한
- 개인정보처리방침·이용약관·숏뉴스 모음을 보류하고 단일 반복 URL의 재발견 점수를 최대 4점으로 제한
- 트렌드 랭킹 알고리즘 버전을 12로 갱신하고 군집·제목·충돌·재발견 회귀 테스트 보강
- `지역 인기: KR / Comedy` 같은 YouTube 수집 범위명을 실제 영상 주제로 사용하지 않고 원래 영상 제목을 사용하도록 정규화
- 인물·기업·프로그램명만 남은 짧은 후보는 사건 맥락이 확인될 때까지 추천 대신 검토로 제한
- 기사 대신 섹션·홈페이지형 원문이 절반 이상인 군집을 보류하고 해당 군집의 재포착 보너스를 제거하되, 일반 원문의 기존 재포착 점수·간격 차등 동작은 유지
- 수집 범위명·단일 대상 제목·섹션 페이지·재포착 보너스 회귀 테스트 추가

# 0.10.26

- 후보 표시 범위와 최소 트렌드 점수를 DB 조회 전에 적용해 상위 100개 선조회 때문에 추천·검토 후보가 누락되던 문제 수정
- 현재 조건에 맞는 전체 후보 수와 실제 불러온 상위 후보 수, 사용자 평가로 숨긴 개수를 글감 목록에 구분해 표시
- 추천·검토·추천만·전체 조회 범위와 점수 필터 순서에 대한 회귀 테스트 추가

# 0.10.25

- 예약 수집에서 모든 출처가 실패했는데도 결과 객체가 존재한다는 이유로 Windows 작업 스케줄러 종료 코드가 성공으로 기록될 수 있던 판정 수정
- 부분 성공과 YouTube 교환 파일 변경 없음은 유효한 수집 결과로 유지하고 종료 코드 회귀 테스트 추가

# 0.10.24

- 글감 목록이 누적 전체가 아니라 최근 분석 범위의 통합 주제라는 점을 화면 진단 영역과 문서에 명확히 표시
- 출처별 저장 전체·최근 72시간·24시간 신규·24시간 갱신·최근 포착 시각을 확인하는 수집 데이터 진단 추가
- NAVER·Daum 확장 검색어가 오래된 역대 고신호 주제에 고정되지 않도록 Google Trends·YouTube 동적 검색어를 현재 분석 범위로 제한
- 프로야구 순위·경기 결과처럼 현재값이 핵심인 주제는 사실 참고 자료 없이 AI 요청서를 생성하지 않도록 차단
- 시점 의존 글에서 현재 자료가 부족할 때 `보는 법` 형태의 일반론으로 대체하지 않고 자료 보강 필요를 반환하도록 AI 요청 규칙 강화
- 일반적인 검색 순위 개선 글은 스포츠 현재 순위로 오인해 차단하지 않도록 문맥 판정 보강

# 0.10.23

- 상단 내비게이션을 실제 고정 위치로 전환해 스크롤 중에도 `수집·사용량` 오른쪽의 Deploy·더보기 컨트롤이 같은 줄과 박스 안에 유지되도록 정렬
- 글감 목록 헤더를 스크롤 영역 안으로 옮기고 CSS Grid로 제목·종합·출처별 건수의 열 폭과 경계선을 동일하게 고정
- 선택 글감 도움말 팝업을 불투명한 배경, 더 큰 글자와 넓은 박스로 조정해 긴 점수 근거도 읽기 쉽게 개선
- `make_safe_upload_zip.bat` 출력 파일명을 `content-trend-tracker_safe_upload.zip`으로 고정하고 기존 날짜·시간 ZIP을 성공 후 정리

# 0.10.22

- 상단 브랜드와 Streamlit Deploy·더보기 컨트롤의 수직 중앙 정렬을 보강하고 네이티브 툴바를 고정 내비게이션 박스 안쪽 라인에 배치
- 오늘의 트렌드 출처 상태를 기본 `metric` 대신 높이와 수직 정렬이 일정한 전용 카드로 변경
- 글감 목록을 `순위·판정·점수·제목 / 종합 / 출처별 건수` 3열 표 형태로 통합하고 선택 행의 경계선을 함께 강조
- 선택 글감의 판정·트렌드·글감 기회·콘텐츠 품질·사실 위험·관련 항목과 근거 진단 카드에 물음표 도움말 추가
- 도움말에 각 지표의 의미뿐 아니라 현재 점수에 반영된 점수 근거·중복·독립 발행처·출처 종류를 함께 표시

# 0.10.21

- `콘텐츠 트렌드 트래커` 브랜드와 주요 메뉴를 한 줄 헤더로 합치고 브랜드는 왼쪽 전용 영역 안에서 정렬
- Streamlit의 Deploy·더보기 컨트롤이 같은 높이에 보이도록 우측 공간을 예약하고 고정 내비게이션을 브라우저 상단 라인으로 이동
- 최신 데이터 수집·분석 및 저장 데이터 재계산 버튼 높이를 YouTube·NAVER·Daum·Google Trends·위키백과 상태 카드와 동일하게 통일
- 오늘의 트렌드 설명과 분석 범위·호출량 안내를 간격이 좁은 2줄 안내 영역으로 묶어 글감 후보가 더 위에서 보이도록 조정

# 0.10.20

- 최신 데이터 수집·분석, 저장 데이터 순위 재계산과 5개 출처 상태를 한 줄로 통합
- 분석 범위와 NAVER·Daum 호출량 안내를 오늘의 트렌드 설명 바로 아래로 이동
- 상단 운영 영역 높이를 줄여 점수순 글감 후보가 화면에 더 빨리 보이도록 조정

# 0.10.19

- 상단 내비게이션 전체를 스크롤 중에도 유지되는 고정형 작업 헤더로 보강
- `콘텐츠 트렌드 트래커` 브랜드명을 메뉴와 분리해 상단 중앙에 더 크게 표시
- 주요 메뉴와 `수집·사용량`을 브랜드 아래 하나의 메뉴 그룹으로 정렬해 시각적 구획을 단순화
- 두 줄 상단 헤더 높이에 맞춰 화면 내 이동 시 콘텐츠가 가려지지 않도록 여백 기준 조정

# 0.10.18

- 왼쪽 사이드바 메뉴와 로컬 수집·호출량을 상단 가로 내비게이션과 `수집·사용량` 팝오버로 이동
- 본문 최대 폭 제한과 좌우 여백을 줄여 넓은 화면에서 상세 정보를 더 많이 표시
- 오늘의 트렌드에서 글감 목록 폭을 넓히고 선택 글감 상세 폭을 조정
- 글감 목록에 순위·판정·점수·제목·종합건수와 NAVER·Daum·YouTube·Google Trends·위키백과 출처별 건수 표시
- Google Trends를 `Google 검색 트렌드`로 명확히 표기하고 YouTube는 API 호출량이 아닌 Parquet 신호 가져오기 상태·저장 건수로 표시
- 순위 조회 결과에 출처 그룹별 건수 집계를 추가하고 기존 군집화·점수 계산·DB 스키마는 유지

# 0.10.17

- 기존 `schema_version 1.0`과 `body_markdown` AI 응답을 계속 지원하면서 새 `schema_version 2.0` 블록형 응답 추가
- 문단·제목·글머리표·번호 목록·인용·이미지 블록을 검증하고 기존 초안 저장 흐름용 Markdown과 이미지 프롬프트로 안전하게 변환
- 잘못된 블록 유형, 목록 항목, 이미지 필드를 `blocks[n].field` 경로로 구체적으로 안내
- 첫 본문 제목이 별도 글 제목과 같으면 중복 저장·복사하지 않고 이미지 위치를 `[이미지 n 삽입 위치]`로 표시
- 블로그 프로필의 기본 출력 형식에 따라 발행 보조 본문을 일반 텍스트 또는 Markdown으로 변환
- 일반 텍스트 변환 시 제목·목록·인용·링크·이미지 위치를 읽기 쉬운 형태로 유지하고 Markdown 표식만 제거
- 기존 초안·AI 세션·발행 기록과 DB 스키마를 변경하지 않는 회귀 테스트 추가

# 0.10.16

- 네이버·티스토리 고정 선택을 여러 블로그 프로필 선택 구조로 확장
- 네이버 블로그, 티스토리, WordPress.com, 자체 설치 WordPress, Google Blogger, Velog, 브런치스토리와 사용자 지정 플랫폼 지원
- 같은 플랫폼의 여러 계정·사이트를 프로필 이름, 로그인 URL, 글쓰기 URL, 기본 출력 형식, 카테고리, 태그와 함께 제한 없이 등록
- 기존 네이버·티스토리 글쓰기 URL을 최초 실행 시 기본 프로필로 안전하게 이전하고 기존 설정값은 호환용으로 유지
- 발행 보조에서 선택한 프로필의 로그인·글쓰기 페이지를 각각 열고 초안 태그와 프로필 기본 태그를 중복 없이 결합
- 발행 완료 기록에 선택한 `blog_profile_id`를 추가해 같은 플랫폼의 여러 블로그를 구분
- 프로필 기본 지정, 숨기기·복원, URL 검증, 중복 이름 방지와 기존 DB 멱등 마이그레이션 회귀 테스트 추가
- 아이디·비밀번호·쿠키는 저장하지 않고 일반 Chrome의 로그인 상태와 비밀번호 관리자를 계속 사용

# 0.10.15

- 오늘의 트렌드 화면 진입 시 저장 데이터 또는 순위 규칙 변경을 감지해도 전체 순위를 즉시 재계산하지 않도록 변경
- 앱 시작 시에는 마지막 계산 결과를 바로 표시하고, 갱신 필요 상태만 안내해 초기 화면 지연 제거
- 자동·수동 최신 데이터 수집 완료 시 기존처럼 군집화와 순위 계산을 함께 수행해 다음 수집에서 변경 규칙 자동 반영
- 즉시 반영이 필요할 때는 기존 `저장된 데이터로 순위만 다시 계산` 버튼을 계속 사용
- 순위를 변경하지 않고 현재 서명·입력 상태만 비교하는 경량 상태 조회 기능과 회귀 테스트 추가

# 0.10.14

- 최근 7일의 자동·수동 수집 실행 이력에서 NAVER·Daum의 실제 요청 수와 재시도 수를 분석하는 기능 추가
- 현재 자동 수집 간격을 기준으로 포털별 하루 예상 호출량, 일일 한도 사용률, 최근 24시간 실제 호출량 표시
- 실행당 평균·최대 요청 수와 재시도율을 표시하고 실제 요청 수에 재시도를 중복 합산하지 않도록 계산
- 출처별 실제 기록 6회 미만은 이론값 우선, 6~23회는 참고값, 24회 이상은 안정적 표본으로 구분
- 실제 최대 관측 요청량과 일일 한도를 기준으로 현재 간격 유지 가능·주의·간격 확대 권장을 표시
- 기존 설정 기반 정상 호출·최대 재시도 계산과 실제 이력 기반 운영 판단을 별도 영역으로 구분
- 실제 사용량 계산, 표본 등급, 재시도 중복 방지, 간격 권장 회귀 테스트 추가

# 0.10.13

- 자동·백그라운드 수집, 화면 수동 수집, 저장 데이터 순위 재계산을 전체 실행 단위로 기록하는 `collection_runs` 추가
- YouTube·Google Trends·위키백과·NAVER·Daum·순위 계산 상세를 같은 `run_id`로 묶는 `collection_run_sources` 추가
- 전체 성공·부분 성공·중복 실행 생략·실패 상태와 요청·재시도·신규·갱신·생략 합계를 정규화된 필드로 저장
- 설정의 자동 수집 스케줄러 아래에 최근 실행 요약 지표, 최근 30건, 선택 실행의 출처별 상세 이력 표시
- 6시간 이상 `running`인 비정상 종료 추정 기록 복구와 기존 실행 기록 보관 설정을 공유하는 기본 90일 정리 추가
- 짧은 DuckDB 프로세스 간 연결 경합에 대한 제한 재시도와 `run_trend_refresh.bat` CRLF 교정을 v0.10.13 변경 이력에 포함
- 실행 이력 스키마·상태 계산·집계·보관·잠금 겹침·백그라운드 진입점·Streamlit 빈 상태와 상태별 표시 회귀 테스트 추가

# 0.10.12

- 화면 수동 수집·저장 데이터 순위 재계산·예약 BAT 실행이 같은 프로젝트 단일 실행 잠금을 공유하도록 보강
- 잠금 파일을 원자적으로 생성하고 PID·시작 시각·실행 진입점을 기록해 동시에 시작한 두 번째 작업은 API 호출 없이 정상 생략
- Windows에서는 프로세스에 신호를 보내지 않고 Win32 조회 전용 핸들로 잠금 PID의 실행 여부를 확인
- 종료된 PID·깨진 잠금은 다음 실행에서 복구하고, 살아 있는 프로세스의 잠금과 다른 소유권 토큰의 잠금은 삭제하지 않도록 보호
- 성공·오류·Streamlit 재실행 여부와 관계없이 현재 소유자가 잠금을 해제하도록 정리
- 예약 실행 BAT가 프로젝트 절대 기준의 가상환경 Python과 스크립트 경로를 따옴표로 실행하고 실제 종료 코드를 그대로 반환하도록 보강
- 중복 실행 차단, 오래된 잠금 복구, 활성 잠금 보존, 예외 해제, 소유권 보호, BAT 경로 인용 회귀 테스트 추가

# 0.10.11

- 설정 화면에서 Windows 작업 스케줄러의 자동 수집 작업을 등록·주기 변경·삭제하는 기능 추가
- 고정 작업명과 강제 갱신 방식을 사용해 30분 작업을 60분으로 바꾸면 기존 작업을 별도 삭제하지 않고 교체
- 현재 등록 여부, 등록 주기, 다음 실행 시각, 작업 상태와 프로젝트 경로 일치 여부 표시
- 포털 탐색어 수·페이지 수와 NAVER·Daum 일일 안전 한도를 기준으로 정상 호출 최소 간격과 최대 2회 재시도 포함 권장 간격 계산
- 선택한 수집 간격의 하루 예상 실행 횟수, 포털별 계획 호출량, 극단적 재시도 호출량과 쿼터 보호 동작 안내
- 작업 스케줄러 명령 생성·갱신·삭제·XML 주기 판독·일일 호출 간격 계산 회귀 테스트 추가

# 0.10.10

- 글쓰기 방향마다 중복 제외 지원 원문 수, 독립 발행처 수, 전체 근거 비율, 대표 원문을 별도로 계산
- 방향을 `주 추천 방향`, `보조 방향`, `단일 근거 참고 방향`으로 구분하고 직접 지원 원문이 없는 방향은 제외 사유와 함께 분리
- 문제 해결·삽질기 방향은 독립 해결 경험 3건과 발행처 2곳 이상일 때만 강한 추천 자격을 부여하고, 1건은 참고 방향으로 유지
- 비교 방향은 실제 비교 원문과 서로 다른 비교 대상 2개가 확인될 때만 허용
- 추적 파라미터 URL, 동일 제목, 매우 유사한 복제 제목을 방향 근거에서 한 번만 계산하고 공식 원문과 복제 기사를 독립 근거 여러 건으로 과대 계산하지 않도록 보강
- 혼합 클러스터의 하위 사건·작성 목적 분포를 표시하고 최신 단일 원문은 대표 근거 선택에만 보조적으로 사용
- 선택 글감 화면에 방향별 근거 등급, 지원 수, 대표 근거 원문, 공식 확인 필요 여부, 낮은 확신·제외 이유 표시
- 단일 삽질기, 독립 삽질기 3건, 비교 근거 유무, 복제 기사, 최신 단일 원문, 추적 URL, AI 요청서 전달 회귀 테스트 보강

# 0.10.9

- 글쓰기 방향용 대표 주제를 최신 단일 원문 제목으로 선택하지 않고, 중복 제외 원문의 35% 이상과 독립 발행처 2곳 이상이 반복 지지할 때만 세부 제목으로 확장
- 동일 URL과 복제 수준의 동일 제목은 글쓰기 의도·근거 집계에서 한 번만 계산
- 공통 제품명만 같고 세부 사건이 여러 갈래인 혼합 클러스터는 특정 삽질기·논란·비교 사례 대신 이슈 구분과 공통 사실 방향만 제안
- 제목이 모호할 때 원문 표현으로 의도를 보완하는 경우에도 서로 다른 발행처의 독립 근거가 필요하도록 강화
- 선택한 글감 화면에 중복 제외 원문 수, 주제명 지지 수, 독립 발행처 수와 혼합 근거 경고 표시
- 단일 최신 원문이 광범위한 군집의 글쓰기 방향을 지배하는 회귀와 복제 비교 제목 중복 집계 회귀 테스트 추가

# 0.10.8

- AI 요청서·결과 검사·편집·발행 단계에서 현재 단계 버튼을 비활성 검정색 대신 왼쪽 선택 메뉴와 같은 기본색으로 표시
- 오늘의 트렌드 상단 5개 출처 상태 카드의 제목과 상태 글자를 카드 크기에 맞게 확대
- 동일 원문의 최초·이전·최근 수집 시각과 누적 포착 횟수를 기존 DB에 안전하게 추가하고 수집 때마다 갱신
- 신규 자료는 감점하지 않으면서 짧은 간격으로 반복 포착된 독립 근거에 최대 10점의 재포착 보너스 적용
- 같은 URL·복제 제목은 재포착 점수에서도 한 번만 계산하고 오래된 원문의 게시 시각은 재수집으로 최신화하지 않도록 유지
- 데이터 변화가 없어도 날짜가 바뀌면 저장 데이터 순위를 하루 한 번 자동 재계산해 기존 최근성 감쇠를 실제 화면 점수에 반영
- 선택한 글감의 트렌드 카드와 관련 원문 상세에서 반복 포착 점수·누적 포착 횟수·최근 재포착 간격 표시
- 기본 72시간 분석 범위 때문에 한 달간 다시 포착되지 않은 주제가 순위에 계속 남지 않는 현재 동작을 문서화

# 0.10.7

- 왼쪽 메뉴에서 선택한 현재 페이지 이름을 고정 상단바에 표시
- 오늘의 트렌드·주제·트렌드·AI 요청서·AI 결과·글 편집·발행 보조·설정 본문에서 중복 페이지 제목 제거
- 고정 상단바 높이에 맞춰 본문 시작 여백과 앵커 이동 여백을 보정해 상세 내용이 상단바에 가려지지 않도록 수정
- 기존 왼쪽 사이드바와 상단바·상세 내용 영역 구분 및 모바일 반응형 배치 유지

# 0.10.6

- 오늘의 트렌드 상단 5개 출처 상태 카드의 높이·안쪽 여백·글자 크기를 줄여 화면 점유 공간 축소
- 선택한 글감의 판정·트렌드·글감 기회·콘텐츠 품질·사실 위험·관련 항목 6개 지표를 넓은 화면에서 한 줄로 배치
- 글감 근거 진단 4개 지표도 같은 밀도의 소형 카드로 통일하고 제목·섹션 간격 정리
- 좁은 화면에서는 주요 지표가 3개씩 자동 줄바꿈되도록 반응형 레이아웃 유지

# 0.10.5

- `오늘의 운세 2026년7월16일`처럼 공백 없이 붙은 한글 날짜도 달력 토큰으로 인식해 구체 주제로 오판하지 않도록 수정
- `오늘의 운세`, 날짜·요일만 있는 후보와 반복형 일일 콘텐츠는 구체 대상이 없으면 자동 보류하고 대표 제목으로 사용하지 않도록 보강
- `삼성생명 운세 서비스 종료`, `서울 폭염경보`처럼 운세·날씨 표현이 있어도 구체 대상과 사건이 있는 후보는 정상 유지
- 랭킹 알고리즘 또는 72시간 입력 데이터가 변경되면 오늘의 트렌드 진입 시 저장 데이터 순위를 자동 갱신해 오래된 추천 판정이 화면에 남지 않도록 개선
- 공백 없는 날짜·일일 운세·구체 사건 보존 회귀 테스트 추가

# 0.10.4

- 글감 상세 화면에 중복 URL을 제외한 실제 근거 수, 중복 추정 수, 독립 발행처 수, 출처 종류를 표시하는 근거 진단 추가
- 전체 원문에서 반복 확인된 구체 대상과 군집이 묶인 이유, 근거 시간 범위, 독립 근거 부족 경고 표시
- 사용자가 후보를 `좋은 글감`, `애매한 글감`, `쓸모없는 글감`, `잘못 묶인 주제`로 평가하고 메모를 저장하는 기능 추가
- 쓸모없음·잘못 묶임으로 평가한 현재 후보를 기본 목록에서 숨길 수 있도록 필터 추가
- 후보 목록에 저장된 사용자 평가 상태를 표시하고 전체 평가 집계를 오늘의 트렌드 화면에 제공
- 평가 결과는 순위를 임의로 바꾸지 않고 별도 `trend_feedback` 테이블에 누적해 후속 품질 조정의 실데이터 근거로 사용
- 실제 운영 DuckDB가 제외된 안전 업로드본에서도 검증할 수 있도록 중복 근거 진단과 평가 저장 회귀 테스트 추가

# 0.10.3

- 날짜·요일·`업데이트 안내`처럼 구체적 대상이 없는 제목은 대표 글감 후보로 사용하지 않고 자동 보류
- 날짜 토큰은 서로 다른 날짜의 같은 표현을 구분하는 보호값으로만 사용하고, 날짜 자체만으로 무관한 원문을 군집화하지 않도록 제한
- 대표 주제의 구체적 대상이 독립 원문 2곳 이상에서 반복 확인되는지 전체 클러스터 상세 근거로 검증
- `GPT-5.6 업데이트`·`캐시워크 정답`처럼 구체 서비스·제품명이 있는 주제는 일반 표현이 포함돼도 정상 유지
- `블로그에 뭘 올릴까` 같은 일반 질문형 문구는 기본 추천·검토 목록에서 숨기고 전체 목록의 보류 후보로만 유지
- pytest 캐시를 프로젝트 폴더에 만들지 않는 `pytest.ini`와 Windows용 `run_tests.bat` 추가
- 기존 `.pytest_manual_check`·`.codex_pytest_*` 등을 한 번에 정리하는 `clean_test_artifacts.bat` 추가

# 0.10.2

- 최신 데이터 수집·분석 또는 저장 데이터 순위 재계산 중에는 사이드바 메뉴 이동을 잠시 잠가 Streamlit 재실행으로 작업 화면이 중간부터 다시 나타나거나 동일 작업이 재시작될 가능성 차단
- 작업 중 다른 메뉴 이벤트가 뒤늦게 전달돼도 `오늘의 트렌드` 화면을 유지하도록 방어 처리
- 사이드바에 현재 실행 중인 작업과 메뉴 잠금 이유를 표시하고 완료·실패 후에는 기존처럼 자동 해제
- 수집·재계산 작업값 정규화와 탐색 잠금 여부 회귀 테스트 추가

# 0.10.1

- AI 요청서 → 결과 검사 → 편집·사실 확인 → 발행 보조의 4단계 진행 표시와 단계 이동 버튼 추가
- 앱을 다시 열어도 저장된 최근 자료팩을 선택해 요청 양식과 사람용 자료팩을 다시 확인 가능
- 요청서에서 사용한 자료팩을 AI 결과 가져오기 화면에 자동 선택하고, 초안 저장 후 해당 초안을 편집·발행 화면에 자동 선택
- AI 결과 검사 이후 자료팩·AI 서비스·붙여넣은 JSON이 바뀌면 이전 검사 결과로 저장하지 못하도록 검증 지문 적용
- 같은 자료팩·AI 서비스·응답을 반복 저장해도 generation과 draft가 중복 생성되지 않도록 멱등 처리
- 같은 초안·플랫폼·발행 URL의 발행 완료 버튼을 반복 눌러도 발행 기록이 중복 생성되지 않도록 보호
- 정답 조회·문제 해결·비교·업데이트·후기형 5개 대표 흐름의 자료팩 생성부터 발행 기록까지 통합 회귀 테스트 추가
- Codex·pytest 임시 폴더와 감사용 임시 DuckDB를 Git 및 배포 ZIP에서 제외하도록 정리 규칙 보강

# 0.10.0

- 개별 주제 예외 대신 제목 중심의 범용 검색 의도 분류 엔진 추가
- 정답 조회, 일정 확인, 문제 해결, 비교·선택, 가격·혜택, 출시·업데이트, 사용 방법, 팩트체크, 후기·반응, 인물·작품, 사건 요약, 일반 설명형 지원
- 출처 종류만으로 방향을 만들지 않고 의도별 허용 방향과 실제 원문 근거 조건을 함께 적용
- 후기·반응은 서로 다른 커뮤니티 발행처가 반복 지지할 때만 장단점 방향을 추가하고 비교·일정·변경·사용법도 근거가 없으면 제외
- 상세 화면에 판단한 검색 의도, 확신도, 판단 이유, 근거 부족으로 제외한 방향 표시
- 의도가 불명확하면 억지로 다섯 개 방향을 채우지 않고 안전한 핵심 정리 1~2개만 제안
- 일정·문제 해결·비교·후기·정책 변경·일반 주제의 회귀 테스트 추가

# 0.9.9

- 캐시워크·오퀴즈·타임스프레드 등 퀴즈 서비스의 정답 조회형 주제를 우선 식별
- 정답 조회형에서는 장단점·생활 영향·커뮤니티 반응 같은 일반 방향을 차단하고 문제별 정답 정리를 첫 방향으로 제시
- 원문에 실제 근거가 있을 때만 새 문제·정답 변경 업데이트와 입력·띄어쓰기 주의 방향 추가
- 퀴즈 앱 사용 후기와 정책 변경은 정답 조회형으로 오분류하지 않도록 회귀 테스트 추가

# 0.9.8

- 실제 72시간 DB 표본에서 날짜·요일·일일 뉴스·제품명 없는 점검 안내가 서로 무관한 문서를 묶고 추천까지 올라오던 문제를 확인해 식별 토큰에서 제외
- 구체 식별자가 없는 동일 제목은 자동 병합 키로 사용하지 않고, 정규 URL이 같은 항목은 일반 토큰이 없어도 중복 후보로 비교
- `GPT-5.6`처럼 문자와 숫자가 함께 있는 구체 버전 검색어는 출처별 제목이 달라도 보수적인 병합 근거로 사용
- 재랭킹 결과에 분석 시간·DuckDB 쓰기 시간·전체 시간을 분리 기록
- 실제 데이터 날짜 오병합, 한국어 조사·버전 보존, 구체 버전 검색어, 동일 사건 제목, 1,000건 합성 성능 회귀 테스트 추가

# 0.9.7

- 사이드바 NAVER·Daum 로컬 호출량의 오늘·월 수치 바로 아래에 사용률 게이지 추가
- 사용률 60% 이상은 주의색, 85% 이상은 경고색으로 표시하고 100%를 넘지 않도록 제한
- Google Trends와 위키백과는 공개된 고정 호출 한도가 없어 기존처럼 로컬 요청 횟수만 표시
- 소량 사용 시에도 게이지가 완전히 사라지지 않도록 최소 표시 폭을 적용하되 정확한 수치는 기존 텍스트로 유지

# 0.9.6

- NAVER와 Daum의 네트워크 수집을 제공자 단위로 동시에 실행하고 DuckDB 저장은 기존처럼 메인 스레드에서 순차 처리
- 포털 요청별 실패를 격리해 일부 요청이 실패해도 성공한 결과는 저장·분석에 계속 반영
- HTTP 429·5xx·타임아웃·일시 연결 오류를 최대 2회 지수형 대기로 재시도하고 인증·DNS 같은 배치성 오류는 빠르게 중단
- 재시도는 남은 일·월 안전 한도 안에서만 허용하고 호출량은 계획 호출 수가 아니라 실제 HTTP 시도 횟수로 기록
- 수집 결과에 계획 요청·실제 요청·성공·실패·생략·재시도 횟수를 표시하고 부분 성공을 별도 상태로 안내
- YouTube Parquet 파일의 경로·크기·수정 시각·가져오기 상한이 이전 실행과 같으면 중복 가져오기를 생략
- 변경 없는 YouTube 생략, 포털 부분 성공 보존, 실제 호출량 기록, NAVER·Daum 제공자 간 동시 실행 회귀 테스트 추가
- 전체 테스트 91개 통과

# 0.9.5

- NAVER 검색 요청을 기본 6개, Daum 검색 요청을 기본 4개의 제한된 스레드로 병렬 처리
- NAVER 50 RPS 기준보다 낮게 유지하고 Daum은 공개된 초당 숫자가 없어 더 보수적인 동시성 적용
- 설정 화면에서 NAVER·Daum 동시 요청 수를 각각 조정 가능
- 포털 요청 결과 순서는 작업 완료 순서와 무관하게 기존 검색어·유형·페이지 순서로 결정론적으로 병합
- 포털 호출량은 실행 전 안전 한도를 확인한 뒤 일·월 카운터에 일괄 기록
- 수천 건 포털 결과를 행별 SELECT/INSERT 대신 staging DataFrame과 단일 UPSERT로 저장
- 군집 후보 생성에 희소 토큰 blocking과 후보 상한을 적용해 반복 토큰으로 인한 비교 폭증 방지
- 문자열 유사도 계산에 `rapidfuzz`를 사용하고 미설치 환경에서는 기존 `SequenceMatcher`로 자동 대체
- 수천 개 군집과 연결 행을 개별 INSERT하지 않고 DataFrame 일괄 삽입으로 순위 저장 시간 단축
- 수집 결과 상세에 포털 요청 수·API 대기 시간·DB 저장 시간을 구분 표시
- 병렬 요청 상한, 페이지 순서 호환, 일괄 저장, 군집·랭킹 회귀 테스트 포함 전체 테스트 87개 통과

# 0.9.4

- 미사용 원본 신호 기본 30일, 수집 실행 기록 90일, 로컬 API 호출량 13개월 보관 정책 추가
- 사용자가 선택한 주제에 연결된 원본은 보관 기간이 지나도 삭제하지 않는 안전 정리 로직 추가
- 설정 화면에서 DB 파일 크기, 전체·최근·연결·오래된 미사용 자료 수와 마지막 정리 시각 표시
- 전체 12,000건 단일 상한 대신 YouTube·NAVER·Daum·Google Trends·위키백과별 분석 상한 적용
- 출처별 분석 상한을 순위 캐시 서명에 포함해 설정 변경 시 자동 재계산
- 원본 정리 시 군집 연결과 빈 군집을 함께 제거하고 순위 캐시를 안전하게 무효화
- source_items 출처·가져오기 시각 인덱스와 보관·출처 상한 회귀 테스트 추가
- 전체 테스트 86개 통과

# 0.9.3

- 수집 결과를 기본 접힘 상태의 요약 패널로 정리하고 총 수집 건수·전체 소요 시간을 제목에 표시
- 펼친 결과에서 YouTube·Google Trends·위키백과·NAVER·Daum별 수집량, 신규·갱신·형식 제외 건수와 소요 시간을 한 줄씩 표시
- 출처별 실패와 통합 순위 계산 시간도 같은 결과 패널에서 구분 표시
- 진행 게이지 문구를 1/7~7/7 단계로 구체화하고 통합 군집·순위 계산 단계를 명확하게 표시
- 저장 데이터 재계산에도 소요 시간과 기존 결과 재사용 여부를 표시
- 대기 작업 실행 코드가 화면 내 두 번 존재하던 중복 블록 제거
- NaN·무한대·문자열·음수 신호값을 0으로 안전하게 보정해 `math domain error` 재발 가능성 차단
- 비정상 수치와 진행 단계·소요 시간 기록 회귀 테스트 추가

# 0.9.2

- 실제 수집 DB 3,631건과 기존 상위 100개를 읽기 전용으로 감사해 검색어 강제 병합, 군집 토큰 누적, 출처량 과대 보상, 범주명 대표 제목 문제 확인
- HTML·URL·추적 파라미터·범위명·일반 토큰을 정리하는 결정론적 정규화 모듈 추가
- 검색어가 같다는 이유만으로 합치지 않고 정규 URL·고유 토큰·한국어 압축 문자열·문장 유사도를 함께 확인하도록 군집 로직 개선
- `horror`, `vtuber`, `moments`, `먹방`, 수집 카테고리 같은 일반 표현만 남으면 투명한 확인 필요 제목과 보류 판정 사용
- 동일 URL과 복제 제목은 한 근거 묶음으로 계산하고 출처 종류·도메인별 근거량을 포화시켜 대량 수집 출처의 영향 제한
- 24시간 반감기 최근성, 뉴스·웹 사실 근거, 커뮤니티 반응, 영상 확산, 검색·위키 보조 신호를 분리한 설명형 점수로 개편
- Google Trends·위키백과 단독 신호와 일반 YouTube 범주는 추천 근거가 되지 않도록 판정 강화
- 실제 근거가 있는 글쓰기 방향만 최대 5개 표시하고 추천 이유와 공식 확인 필요 여부를 함께 제공
- 수집 입력이 바뀌지 않은 순위 재계산은 기존 결과를 재사용하도록 개선
- `source_items.normalized_url`을 기존 행 삭제 없이 추가·보정하는 호환 마이그레이션과 URL 인덱스 추가
- 다크 테마의 글자 계층·최대 본문 폭·사이드바 간격·상태 표시·후보 목록 스크롤·상세 지표와 원문 카드 정리
- 정규화, 중복 URL, 한국어 유사 사건, 범주명 거부, 출처량 정규화, 최근성, 공개 신호, 글쓰기 방향 전달, 마이그레이션, 순위 재사용 회귀 테스트 추가
- 전체 테스트 78개 통과

# 0.9.1

- 위키백과·Daum 결과 중 제목이 특수문자뿐이거나 필수 식별값이 빠진 항목 하나 때문에 출처 전체가 실패하던 문제 수정
- 외부 신호 가져오기에서 형식이 불완전한 개별 항목만 건너뛰고 정상 항목은 계속 저장하도록 부분 성공 처리 강화
- NAVER·Daum 검색을 여러 페이지까지 조회할 수 있도록 페이지 파라미터와 호출량 사전 계산 추가
- 기본 포털 탐색 범위를 최대 50개 검색어·검색어당 2페이지로 확대해 출처별 실행당 최대 약 200회 호출
- 설정 화면에서 포털 탐색어 수·페이지 수·페이지당 결과 수를 조정하고 예상 호출량을 즉시 표시
- 사이드바 사용량의 오늘/월 수치를 각각 한 줄로 분리하고 메뉴와 사용량 영역 사이 여백 축소
- 통합 순위 입력 상한을 5,000건에서 12,000건으로 확대
- 전체 테스트 62개 통과

# 0.9.0

- 카카오디벨로퍼스 Daum 검색 REST API 연동 추가
- Daum 웹문서와 카페 결과를 최신순으로 수집해 통합 군집·순위·글쓰기 방향에 반영
- 카카오 REST API 키 상태와 Daum 일·월 로컬 호출량을 사이드바·설정 화면에 표시
- Daum 일 50,000회, 카카오 앱 전체 월 3,000,000건 무료 쿼터 상한 적용
- Daum 인증·네트워크·쿼터 오류를 다른 출처 수집과 격리
- 전체 테스트 59개 통과

# 변경 기록

## 0.8.9 - 2026-07-15

- Google Trends 한국 `Trending now` 공식 RSS 수집 어댑터 추가
- Google 급상승 검색어와 구간형 검색량을 저장하고 상위 검색어를 같은 실행의 NAVER 뉴스·블로그 탐색어로 자동 활용
- Wikimedia Analytics API의 한국어 위키백과 일간 인기 문서·조회수·순위 수집 추가
- 위키백과 단독 신호는 추천 글감으로 올리지 않고 다른 출처와 결합될 때 관심도 검증용으로만 반영
- Google 검색 급상승과 위키백과 조회 관심을 트렌드 점수와 글쓰기 방향 추천에 반영
- 외부 데이터 요청량을 제공자별 일·월 단위로 기록하는 범용 로컬 사용량 기능 추가
- 왼쪽 메뉴 아래에 NAVER·Google Trends·위키백과 로컬 호출량을 상시 표시
- 수집 완료 후 자동으로 화면을 다시 실행해 브라우저 수동 새로고침 없이 사용량과 결과가 즉시 갱신되도록 수정
- 설정 화면에서 Google Trends·위키백과 사용 여부와 최대 수집 개수를 조정할 수 있도록 추가
- 출처별 실패 격리를 네 가지 수집원 전체로 확장

## 0.8.8 - 2026-07-15

- 고정된 6~7개 문장 템플릿 대신 원문 제목·설명·출처 구성을 분석하는 로컬 글쓰기 방향 추천기로 변경
- 뉴스·블로그·YouTube 포함 여부와 발표·후기·비교·일정·논란·확산 표현에 따라 방향별 점수를 계산
- 실제 근거가 있는 `최신 변화`·`실사용 반응`·`비교`·`사실 확인`·`일정 정리`·`확산 이유`·`출처 교차 분석` 등을 최대 6개 추천
- `인기영상:KR:전체` 같은 수집 범위명을 실제 주제로 오인하지 않도록 일반 검색 범위 판별 강화
- 일반 범위가 같은 서로 무관한 YouTube 영상들이 하나의 글감으로 합쳐지지 않도록 군집 기준 수정
- 추천 방향 안내 문구를 실제 수집 데이터에 따라 달라진다는 설명으로 변경

## 0.8.7 - 2026-07-15

- 사이드바 폭과 내부 여백을 조정하고 브랜드명을 한 줄로 고정해 제목 잘림 방지
- 브랜드명과 첫 메뉴 버튼 사이 간격을 늘려 왼쪽 메뉴 구획을 명확하게 정리
- 글감 선택 상자를 제거하고 현재 필터의 전체 후보를 좁은 스크롤 목록에 표시
- 후보 한 줄 전체를 클릭 가능한 버튼으로 만들고 현재 선택 항목을 강조색으로 표시
- 화면을 왼쪽 글감 목록·오른쪽 상세의 2단 구조로 변경
- 선택한 글감의 점수, 글쓰기 방향, 선정 근거, 품질 판단, 관련 원문을 오른쪽에 한 번에 표시

## 0.8.6 - 2026-07-15

- 모든 화면에 반복되던 큰 `콘텐츠 트렌드 트래커 + 블로그 제작 보조기` 제목과 설명 제거
- 왼쪽 사이드바 상단에는 `콘텐츠 트렌드 트래커` 브랜드명만 표시
- 기본 라디오 메뉴를 여백이 있는 전체 너비 버튼형 메뉴로 변경
- 현재 선택된 메뉴는 기본 강조색으로 표시하고 다른 메뉴는 보조 버튼으로 구분
- 사이드바 버튼을 왼쪽 정렬하고 간격·모서리·호버 효과를 정리해 메뉴 가독성 개선

## 0.8.5 - 2026-07-15

- 세로로 길게 늘어진 상위 30개 라디오 목록을 검색 가능한 단일 선택 상자로 변경
- 선택한 글감의 글쓰기 방향을 첫 번째 탭에 바로 표시해 추가 스크롤을 최소화
- 상세 화면을 `글쓰기 방향`·`선정 근거`·`관련 원문`·`전체 순위` 탭으로 분리
- 전체 순위 표는 필요할 때만 여는 탭으로 이동하고 높이를 제한해 화면 가독성 개선
- 선택한 순위·판정·주제를 상세 상단에 명확히 표시하고 AI 요청서 버튼을 넓게 배치

## 0.8.4 - 2026-07-15

- NAVER API HUB 콘솔의 실제 한도에 맞춰 검색 API 일 25,000회·월 775,000회 제한을 모두 적용
- 프로그램 기본 한도를 현재 무료 최대치인 일 25,000회·월 775,000회로 변경
- 요청 시도마다 일간·월간 로컬 카운터를 함께 기록하고 어느 한도든 초과 전 네이버 수집 차단
- 기존 0.8.3의 월 20,000회 기본값을 최초 실행 시 월 775,000회로 자동 전환하는 설정 마이그레이션 추가
- 설정·오늘의 트렌드 화면에 오늘·이번 달 사용량과 무료 최대 한도를 함께 표시
- 현재 Data Lab 월 최대 50,000회 0원 정책은 안내만 하고 실제 API 호출은 하지 않음

## 0.8.3 - 2026-07-15

- NAVER 검색 API 호출 한도를 일 단위가 아닌 API Key 기준 월 단위로 정정
- 잘못된 공식 일 25,000회 안내와 프로그램 일일 안전 한도 제거
- 검색 API 로컬 카운터를 월간 호출량만 기록하도록 단순화
- 공식 월 최대 775,000회·최대 50 RPS·50%/80%/100% 이메일 알림 안내 반영
- Data Lab은 현재 미사용이며, 향후 월 30,000회 초과 한시적 무료 구간은 명시 동의 후에만 사용한다는 정책 명시

## 0.8.2 - 2026-07-15

- NAVER API HUB 검색 API 호출량 로컬 안전 카운터 최초 추가(0.8.3에서 공식 정책에 맞춰 월 단위로 정정)
- 기본 월 20,000회 안전 한도와 공식 월 775,000회 상한 적용
- 이번 실행 예정 호출이 안전 한도를 넘으면 네이버 수집만 차단하고 YouTube·저장 데이터 분석은 계속
- 오늘의 트렌드와 설정 화면에 오늘·이번 달 호출 수와 실행 예상 호출 수 표시
- 검색어 트렌드·쇼핑 인사이트의 한시적 무료 유료 구간은 현재 프로그램이 사용하지 않음을 명시
- 향후 검색어 트렌드 API 추가 시 기본 무료 구간 초과 전에 별도 동의를 요구하도록 정책 기록

## 0.8.1 - 2026-07-14

- 네이버 DNS·도메인·인증 오류가 발생해도 YouTube 가져오기와 기존 데이터 순위 계산을 계속하도록 출처별 실패 격리
- `최신 데이터 수집·분석` 결과를 YouTube·네이버·순위 계산 단계별로 구분해 표시
- NAVER API HUB 도메인 해석 실패 시 인터넷·DNS·VPN·보안 프로그램 점검 안내 추가
- 자동 새로고침 BAT도 한 출처 실패 시 다른 출처와 저장 데이터 분석을 계속하도록 개선
- 접힌 선택 상자 대신 상위 순위가 세로로 보이는 주제 선택 목록으로 변경
- 순위 목록에서 주제를 고르면 해당 원문·점수 이유·여러 글쓰기 방향이 즉시 바뀌도록 안내 강화

## 0.8.0 - 2026-07-14

- 실제 영상 제목보다 YouTube 주제명·검색 키워드를 우선해 통합 주제 제목 정리
- 단일 영상, 해시태그 과다, 반복 특수문자, 라이브·쇼츠성 표현을 감점하는 콘텐츠 품질 점수 추가
- 트렌드 순위에 `추천`·`검토`·`보류` 판정과 콘텐츠 품질 열 추가
- 기본 화면에서 보류 후보를 숨기고 최소 트렌드 점수를 조정하는 필터 추가
- 주제 상세에 콘텐츠 품질 판단 이유와 보류 경고 표시
- 기존 DuckDB에 품질 관련 컬럼을 자동 추가하는 호환 마이그레이션 추가

## 0.7.2 - 2026-07-14

- 오늘의 트렌드 점수순 표에 대표 영상·뉴스·블로그 `원문 보기` 링크 열 추가
- 통합 주제별 대표 원문은 신호값과 최신 시각을 기준으로 선택
- 주제 상세의 관련 항목 버튼을 `영상 보기`·`뉴스 보기`·`블로그 보기`로 구분

## 0.7.1 - 2026-07-14

- 사용하지 않는 기존 네이버 개발자센터 검색 API 호환 분기 제거
- 네이버 뉴스·블로그 수집기를 NAVER API HUB 주소와 인증 헤더로 고정
- `NAVER_API_MODE` 설정과 레거시 전용 문서·테스트 제거
- 설정 화면과 자동 수집 스크립트를 API HUB 전용 흐름으로 단순화

## 0.7.0 - 2026-07-14

- 첫 화면을 `오늘의 트렌드`로 변경하고 자동 글감 순위를 메인 흐름으로 전환
- NAVER API HUB와 기존 개발자센터 검색 API를 모두 지원하는 뉴스·블로그 어댑터 및 `.env` 키 분리 추가
- YouTube 상위 주제와 설정 탐색어를 조합한 최근 뉴스·블로그 수집 추가
- AI API 없이 제목·설명 토큰 유사도로 여러 출처의 주제를 자동 통합
- 최근성·반복 언급·출처 다양성·YouTube 상승세·출처 수 기반 트렌드 점수 추가
- 정보성 표현과 사실 확인 위험을 반영한 글감 기회 점수 추가
- 주제 상세에서 관련 영상·뉴스·블로그와 점수 산정 이유 표시
- 규칙 기반 글쓰기 방향 추천 후 기존 AI 요청서 화면으로 바로 연결
- 화면과 동일한 수집·분석을 실행하는 `run_trend_refresh.bat` 추가

## 0.6.0 - 2026-07-14

- 첫 화면을 `글 만들기`로 변경하고 주제 관리보다 글 제작 시작을 우선하도록 메뉴 재구성
- 저장된 주제가 없어도 글감·작성 방향을 입력해 AI 자료팩을 바로 생성하는 빠른 시작 추가
- 빠른 시작 시 내부 작업 주제를 자동 생성하거나 같은 정규화 제목의 기존 주제를 재사용
- 빠른 시작 자료팩은 원치 않는 트렌드·공식 자료를 자동 포함하지 않고 나중에 연결 가능
- 사용자 화면과 자료팩의 `통합 주제` 표현을 `주제·트렌드`와 `글 주제` 중심으로 단순화

## 0.5.0 - 2026-07-14

- 주제별 공식 기관·기업 발표·공공데이터·뉴스·사용자 자료 직접 등록 기능 추가
- 동일 주제의 같은 URL은 중복 생성하지 않고 기존 참고 자료 갱신
- 등록한 참고 자료 수정·보관 및 원본 URL 열기 기능 추가
- AI 자료팩에서 트렌드 신호와 사실 참고 자료를 각각 선택하도록 분리
- 자료팩과 AI 프롬프트에서 트렌드 신호와 사실 근거의 역할을 명확히 구분
- 선택한 사실 참고 자료를 기존 출처 ID·URL 검증 흐름에 자동 연결
- 기존 DB 실행 시 `topic_references` 테이블을 자동 생성하도록 확장

## 0.4.0 - 2026-07-14

- AI 결과의 출처 ID와 URL을 선택한 자료팩 기준으로 교차 검사
- 자료팩에 없던 출처·URL과 본문 내 임의 URL은 초안 저장 전 오류로 차단
- 숫자·가격·정책 표현이 있으나 사실 확인 목록이 비어 있으면 경고
- AI가 확인 완료로 출력해도 사용자 확인 전에는 모든 사실 확인 항목을 미확인으로 저장
- 사실 확인 항목별 미확인·확인 완료·수정 필요 상태, 확인 메모, 근거 URL 관리 추가
- 모든 사실 확인이 끝나면 주제 상태를 발행 준비로 자동 전환
- 미확인 항목이 남은 글은 발행 완료 기록 전에 경고와 사용자 확인 요구

## 0.3.0 - 2026-07-14

- 통합 주제의 연결 신호를 떠오르는 주제·최근 영상·콘텐츠 아이디어로 구분 표시
- 각 신호의 조회수, 증가량, 시간당 조회수, 주제 점수와 원본 링크 표시
- AI 자료팩에 포함할 트렌드 근거를 사용자가 직접 선택하는 기능 추가
- 선택 근거의 개수와 주요 수치를 규칙 기반으로 자동 요약
- 선택한 근거만 자료팩과 Gemini·ChatGPT 공용 프롬프트에 저장
- YouTube 신호를 사실 보증 자료로 단정하지 않도록 프롬프트 규칙 강화

## 0.2.0 - 2026-07-14

- YouTube 트래커의 Parquet 교환 파일을 기본 신호 출처로 연결
- 기존 YouTube DuckDB 직접 읽기를 수동 fallback으로 유지
- Parquet 스키마, 파일 상태, 마지막 정상 가져오기 표시
- 동일 신호 재가져오기 시 중복 생성 대신 갱신

## 0.1.0 - 2026-07-14

- 독립된 콘텐츠 트렌드 트래커 MVP 생성
- 메인 주제 직접 등록과 관심·제작 상태 관리 추가
- 기존 YouTube DuckDB 읽기 전용 어댑터 추가
- Gemini·ChatGPT 공용 자료팩과 JSON 출력 프롬프트 추가
- AI 결과 형식 검사와 초안 저장 추가
- Markdown·HTML 미리보기와 수정 이력 추가
- 네이버 블로그·티스토리 수동 발행 보조 추가
- 안전 업로드 ZIP 생성 BAT 추가
